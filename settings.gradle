pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id  'io.quarkus'  version '2.13.1.Final'
    }
}

rootProject.name = 'bug-report'
